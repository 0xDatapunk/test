import { ContractFactoryLike } from './types';
export declare function deployContractMock<T extends ContractFactoryLike>(path: string, opts?: {
    provider?: any;
    address?: string;
}): Promise<ReturnType<T['deploy']> & {
    smocked: any;
}>;
