import { Signer } from 'ethers';
import { ContractFactoryLike } from '../types';
export declare function simpleDeploy<T extends ContractFactoryLike>(name: string, args: Parameters<T['deploy']>): Promise<ReturnType<T['deploy']>>;
export declare function deployUsingFactory<T extends ContractFactoryLike>(signer: Signer, factory: T, args: Parameters<T['deploy']>): Promise<ReturnType<T['deploy']>>;
export declare function deployUsingFactoryAndVerify<T extends ContractFactoryLike>(signer: Signer, factory: T, args: Parameters<T['deploy']>): Promise<ReturnType<T['deploy']>>;
