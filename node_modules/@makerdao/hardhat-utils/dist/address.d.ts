import { Signer } from 'ethers';
/**
 * Get address of the future contract
 * @param signer
 * @param offset - default (0) means next address
 * @returns
 */
export declare function getAddressOfNextDeployedContract(signer: Signer, offset?: number): Promise<string>;
export declare function getRandomAddress(): Promise<string>;
export declare function getRandomAddresses(n?: number): Promise<string[]>;
